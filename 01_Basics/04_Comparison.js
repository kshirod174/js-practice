console.log(2>1);
console.log(2>=1);
console.log(2<1);
console.log(2 == 1);
console.log(2 != 1);

console.log(null > 0);
console.log(null == 0);
console.log(null >= 0); //in JS comparison and equality check are two different things. hence always keep both the value in one type format before comparing
                        // == works differently rather than other comparison operatoe
                        //=== is used for strict check - it checks value as well as data types of both the values
                        //summary is always keep both the variables in one single data types
                        